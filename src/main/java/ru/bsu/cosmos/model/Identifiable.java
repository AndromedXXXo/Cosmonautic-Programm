package ru.bsu.cosmos.model;

/**
 * Интерфейс для объектов, имеющих уникальный идентификатор.
 * Демонстрирует принцип абстракции — общее свойство наделяется
 * собственным контрактом, не зависящим от иерархии наследования.
 */
public interface Identifiable {
    long getId();
}
